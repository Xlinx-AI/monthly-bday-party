# Vercel Postgres (получите на dashboard.vercel.com)
# IMPORTANT: POSTGRES_URL should use the POOLED connection string (contains "-pooler." in hostname)
# This is required for the @vercel/postgres client in serverless environments
# If you only have a direct URL (without -pooler), the app will attempt to convert it automatically.
POSTGRES_URL="postgres://default:xxx@xxx-pooler.postgres.vercel-storage.com/verceldb?sslmode=require"

# Optional: Provide direct URLs from Prisma/Vercel if available. They will be used for migrations
# and can also be auto-converted to pooled connections at runtime when needed.
# POSTGRES_URL_NON_POOLING="postgres://default:xxx@xxx.postgres.vercel-storage.com/verceldb?sslmode=require"
# POSTGRES_PRISMA_URL="postgres://default:xxx@xxx-pooler.postgres.vercel-storage.com/verceldb?pgbouncer=true&connect_timeout=15"
# DATABASE_URL="postgres://default:xxx@xxx.postgres.vercel-storage.com/verceldb"

# JWT Secret (сгенерируйте: openssl rand -base64 32)
JWT_SECRET="your-secret-key-change-me"

# ЮKassa
YOOKASSA_SHOP_ID="123456"
YOOKASSA_SECRET_KEY="test_yookassa_secret_key"

# Base URL
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Demo mode (set to true для mock-режима без реальных платежей)
TWELVEDR_ENABLE_PAYMENT_MOCKS=true
