# Vercel Postgres (получите на dashboard.vercel.com)
# IMPORTANT: POSTGRES_URL must use the POOLED connection string (contains "-pooler." in hostname)
# This is required for the @vercel/postgres client in serverless environments
POSTGRES_URL="postgres://default:xxx@xxx-pooler.postgres.vercel-storage.com/verceldb?sslmode=require"

# Optional: For migrations (drizzle-kit), you can optionally provide direct connection URLs
# If not set, migrations will use POSTGRES_URL (pooled, which also works for migrations)
# POSTGRES_URL_NON_POOLING="postgres://default:xxx@xxx.postgres.vercel-storage.com/verceldb?sslmode=require"
# POSTGRES_PRISMA_URL="postgres://default:xxx@xxx.postgres.vercel-storage.com/verceldb?sslmode=require"

# JWT Secret (сгенерируйте: openssl rand -base64 32)
JWT_SECRET="your-secret-key-change-me"

# ЮKassa
YOOKASSA_SHOP_ID="123456"
YOOKASSA_SECRET_KEY="test_yookassa_secret_key"

# Base URL
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Demo mode (set to true для mock-режима без реальных платежей)
TWELVEDR_ENABLE_PAYMENT_MOCKS=true
