# Monthly Birthday Club

**Monthly Birthday Club (MBC)** — это веб-платформа для организации ежемесячных тематических вечеринок, приуроченных ко дню рождения пользователя. Платформа объединяет «именинников» и гостей, автоматизирует платежи и предоставляет электронные билеты.

---

## 🚀 Технологический стек

- **Frontend:** React 19 + Next.js 16 (App Router, TypeScript)
- **Backend:** Serverless API Routes (Next.js)
- **База данных:** Vercel Postgres
- **ORM:** Drizzle ORM
- **Авторизация:** JWT (httpOnly cookies)
- **Платежи:** Stripe
- **Стилизация:** Tailwind CSS v4
- **Деплой:** Vercel

---

## 📋 Возможности MVP

1. **Регистрация и авторизация:**
   - Пользователь может зарегистрироваться с email, паролем и датой рождения
   - JWT-based аутентификация с безопасными httpOnly cookies

2. **Профиль и интересы:**
   - Редактирование имени, телефона, города и биографии
   - Управление списком интересов (добавление через запятую)

3. **Создание мероприятий:**
   - Раз в месяц пользователь может создать мероприятие в день своего рождения
   - Выбор темы из своих интересов
   - Указание названия, описания, даты, места, стоимости и максимального количества гостей
   - Генерация уникального кода приглашения

4. **Приглашение гостей:**
   - Генерация ссылки-приглашения с кодом
   - Гости могут присоединиться к событию по коду

5. **Оплата билетов:**
   - Интеграция с Stripe Payment Intents API
   - После оплаты гость получает электронный билет

6. **Лента событий:**
   - Просмотр всех предстоящих мероприятий
   - Фильтрация по городу и интересам

---

## 🛠️ Установка и запуск

### Требования

- Node.js 18+
- npm или yarn
- Vercel аккаунт (для Postgres)
- Stripe аккаунт (для платежей)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd monthly-birthday-club
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env.local`:

```bash
cp .env.example .env.local
```

Заполните переменные:

```env
# Database (получите на Vercel после создания Postgres DB)
POSTGRES_URL="postgres://..."
POSTGRES_PRISMA_URL="postgres://...?pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgres://..."

# JWT Secret (сгенерируйте случайную строку)
JWT_SECRET="your-secret-key-here"

# Stripe (создайте на stripe.com)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
```

### 4. Настройка базы данных

**Создайте Vercel Postgres:**

1. Перейдите на [Vercel Dashboard](https://vercel.com/dashboard)
2. Создайте новый Storage → Postgres
3. Скопируйте переменные окружения в `.env.local`

**Примените схему базы данных:**

```bash
npm run db:migrate
```

Это создаст все необходимые таблицы: `users`, `interests`, `user_interests`, `events`, `event_guests`.

### 5. Запуск в dev-режиме

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

---

## 📂 Структура проекта

```
src/
├── app/                    # Next.js App Router pages
│   ├── api/                # API routes
│   │   ├── auth/           # Авторизация (register, login, logout, me)
│   │   ├── user/           # Профиль и интересы
│   │   ├── events/         # CRUD событий, лента, приглашения
│   │   └── payments/       # Stripe платежи
│   ├── dashboard/          # Личный кабинет
│   ├── events/             # Страницы событий (лента, создание, детали)
│   ├── profile/            # Редактирование профиля
│   ├── register/           # Регистрация
│   ├── login/              # Вход
│   ├── layout.tsx          # Root layout
│   ├── page.tsx            # Главная страница
│   └── globals.css         # Глобальные стили
├── components/
│   └── ui/                 # Переиспользуемые UI компоненты
├── db/
│   ├── schema.ts           # Drizzle ORM схема БД
│   └── index.ts            # DB connection
└── lib/
    ├── auth.ts             # JWT и session helpers
    ├── password.ts         # bcrypt
    ├── utils.ts            # Утилиты (generateId, formatDate и т.д.)
    └── validation.ts       # Zod схемы валидации
```

---

## 🔒 Безопасность

- **Пароли:** хешируются с помощью bcryptjs (10 rounds)
- **JWT:** хранятся в httpOnly cookies, защищены от XSS
- **Stripe Webhook:** проверка подписи для безопасной обработки платежей
- **Валидация:** все входящие данные валидируются через Zod

---

## 🎨 Дизайн

- Адаптивный UI для всех устройств
- Tailwind CSS v4 с inline-конфигурацией
- Минималистичный дизайн в фиолетово-серых тонах
- Использование семантических компонентов (Button, Input)

---

## 📦 API Endpoints

### Авторизация

- `POST /api/auth/register` — Регистрация
- `POST /api/auth/login` — Вход
- `POST /api/auth/logout` — Выход
- `GET /api/auth/me` — Текущий пользователь

### Профиль

- `PUT /api/user/profile` — Обновить профиль
- `GET /api/user/interests` — Получить интересы
- `PUT /api/user/interests` — Обновить интересы

### События

- `POST /api/events/create` — Создать мероприятие
- `GET /api/events/feed` — Лента событий (query: city, interestId)
- `GET /api/events/my` — Мои мероприятия
- `GET /api/events/[id]` — Детали мероприятия
- `PATCH /api/events/[id]` — Обновить мероприятие (только хост)
- `POST /api/events/[id]/join` — Присоединиться к событию
- `GET /api/events/invite?code=XXX` — Получить событие по коду приглашения

### Платежи

- `POST /api/payments/create-intent` — Создать Stripe Payment Intent
- `POST /api/payments/webhook` — Stripe webhook (обновление статуса оплаты)

---

## 🚀 Деплой на Vercel

1. Импортируйте проект на [Vercel](https://vercel.com)
2. Добавьте все переменные окружения в настройках проекта
3. Настройте Stripe Webhook URL: `https://your-domain.vercel.app/api/payments/webhook`
4. Деплой автоматически выполнится при пуше в `main` или создании PR

---

## 🧪 Тестирование

### Регистрация нового пользователя

1. Перейдите на `/register`
2. Заполните форму (email, пароль, дата рождения)
3. После успешной регистрации вы будете перенаправлены в `/dashboard`

### Добавление интересов

1. Перейдите в `/profile`
2. Введите интересы через запятую: `караоке, настольные игры, йога`
3. Нажмите "Сохранить интересы"

### Создание мероприятия

1. Перейдите в `/dashboard` и нажмите "Создать мероприятие"
2. Выберите интерес из списка
3. Заполните название, дату, место, цену билета и количество гостей
4. Нажмите "Создать мероприятие"

### Приглашение гостей

1. Скопируйте ссылку-приглашение из карточки мероприятия в дашборде
2. Отправьте ссылку друзьям
3. Друзья могут перейти по ссылке, ввести код и присоединиться
4. После присоединения они могут оплатить билет

---

## 📝 Дальнейшие улучшения (вне MVP)

- **Автоподбор гостей:** алгоритм подбора пользователей по интересам и городу
- **Email-рассылка:** отправка приглашений на почту
- **QR-коды:** генерация и сканирование билетов
- **Система отзывов:** рейтинги мероприятий и пользователей
- **Push-уведомления:** напоминания о предстоящих событиях
- **Мультиязычность:** поддержка английского и других языков
- **Админ-панель:** модерация событий и пользователей

---

## 📄 Лицензия

MIT License

---

## 👨‍💻 Автор

Создано AI SWE Agent по техническому заданию.

Для вопросов и предложений создавайте Issue в репозитории.
